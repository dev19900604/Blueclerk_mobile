/**
 * @format
 */

import { AppRegistry } from 'react-native';
import Blueclerk from './src/App';
import { name as appName } from './app.json';

AppRegistry.registerComponent(appName, () => Blueclerk);
